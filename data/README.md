# data/ — prepared tables

**Purpose:** clean tables produced by the fetch scripts (works, authors, sources, event table). Not checked in (gitignored) — only this README and `schemas/` live in the repo.
**Owner:** Lennart
**Input:** raw data from `fetch/`
**Output:** tables for `logic/`, `topics/`, `nets/`; schema docs per table in `schemas/`
