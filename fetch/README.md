# fetch/ — retrieval scripts

**Purpose:** fetch data from OpenAlex, DOAJ, and ORCID (free sources only) and store it as clean tables. Access via `.env` (see `.env.example`), no keys in the repo.
**Owner:** Lennart (lead), Kevin (support on filtering strategy) — confirm at kickoff
**Input:** OpenAlex/DOAJ/ORCID APIs
**Output:** tables in `data/` + schema docs in `data/schemas/`
