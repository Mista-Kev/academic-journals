# report/ — report & defense

**Purpose:** result tables, figures (scale: 1.0 = chance), report text, and defense slides. Set up the outline early; each role writes its methods section continuously.
**Owner:** all
**Input:** results from `nets/` and `stats/`
**Output:** report + slides for the defense (M5)
